version: "2"

run:
  timeout: 5m
  tests: true
  # --concurrency=2 caps parallel analyser goroutines; k8s.io v0.35 expands
  # the module graph significantly and was OOM-killing the CI runner.
  concurrency: 2

linters:
  enable:
    - errcheck       # check unchecked errors
    - govet          # correctness checks
    - ineffassign    # detect ineffectual assignments
    - unused         # check for unused code
    - misspell       # spelling mistakes in comments/strings
    - gosec          # security checks
    - noctx          # http requests without context
    - bodyclose      # http response body closure
    - prealloc       # slice pre-allocation suggestions
    - unconvert      # unnecessary type conversions
    - unparam        # unused function parameters
  settings:
    gosec:
      excludes:
        - G107  # url provided to HTTP request as taint input (we trust CR content)

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/amcheste/claude-teams-operator

issues:
  exclude-rules:
    # Test files get more relaxed rules
    - path: _test\.go
      linters:
        - gosec
        - errcheck
    # Generated deepcopy files
    - path: zz_generated
      linters:
        - all
